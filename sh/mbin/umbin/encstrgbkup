#!/bin/sh -e
. /usr/subr/out
[ "$#" -eq 0 ] && exit 1
for dir in "$@"; do
	[ -d "$dir" ] || perrexit "$dir: not a dir" 2
done
for dir in "$@"; do
	tar czf /mnt/strg/ectd/bkup/"$(echo "$dir" | sed 's|/|_|g')--$(date +%Y-%m-%d_%T).tar.gz" "$dir" >/dev/null
done
