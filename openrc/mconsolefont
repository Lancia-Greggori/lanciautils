#!/usr/bin/openrc-run
depend(){
	provide consolefont
	after local
}
start(){
	for x in $(find /etc/runlevels -name '*.tty[0-9]'); do
		setfont \
			/usr/share/kbd/consolefonts/solar24x32.psfu.g
			-C /dev/${x##*.}
	done
}
