LSR="$HOME/.lsr"
PATH="$LSR/lmbin:$LSR/bin:$LSR/sbin:$PATH"
LOGD="$HOME/.logs"
TERMINAL=st
export LSR LOGD PATH TERMINAL

. "$HOME/.bashrc"

pgrep -f transmission-daemon 1>/dev/null || transmission-daemon &
