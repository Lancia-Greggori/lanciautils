#!/bin/sh -eu

[ "$#" -ne 1 ] && exit 1

SINTAXIS_CLASS_KEYWORD='YrbPuc'
CURL_OUTPUT="$(curl --silent --user-agent "$USER_AGENT" --location "https://google.com/search?q=$1+significado")"

echo "$CURL_OUTPUT" | grep -o "class=\"$SINTAXIS_CLASS_KEYWORD\">[^<]\+" | sed 's/[^>]*>//'
