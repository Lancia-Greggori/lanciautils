#!/bin/sh -eu

[ "$#" -ne 3 ] && {
	echo "USAGE: ${0##*/} [source file] [target] [perm]"
	exit 1
}
[ "$(id -u)" -ne 0 ] && exit 2

exec echo "$(readlink -f "$1"):$(readlink -f "$2"):$3" >> /letc/global-filelist
